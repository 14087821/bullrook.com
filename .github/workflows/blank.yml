# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portal DYDX Chain</title>
    <style>
        /* Estilos CSS BÁSICOS para la estructura */
        body { font-family: sans-serif; margin: 0; padding: 0; background-color: #f4f7f9; color: #333; }<script>
    const chainId = 'dydx-mainnet-1'; // ID de la cadena que identificamos

    document.getElementById('connectWalletBtn').addEventListener('click', async () => {
        const statusElement = document.getElementById('walletStatus');

        // 1. Verificar si Keplr está instalado
        if (typeof window.keplr === 'undefined') {
            statusElement.textContent = 'Estado: ¡Instala Keplr para continuar!';
            alert('Keplr no está instalado. Por favor, instálalo para conectarte a la dYdX Chain.');
            return;
        }

        try {
            // 2. Solicitar permiso para conectarse a la dYdX Chain
            await window.keplr.enable(chainId);
            statusElement.textContent = 'Estado: ✅ Conectado con éxito.';
            
            // 3. Obtener la dirección del usuario (opcional, pero útil)
            const offlineSigner = window.keplr.getOfflineSigner(chainId);
            const accounts = await offlineSigner.getAccounts();
            
            // Mostrar la dirección en el estado (o en otro lugar de la página)
            statusElement.innerHTML = `Estado: ✅ Conectado. Dirección: <strong>${accounts[0].address.substring(0, 10)}...</strong>`;
            
            alert('¡Conexión exitosa a la dYdX Chain!');

        } catch (error) {
            // 4. Manejar errores de conexión
            console.error('Error al conectar la billetera:', error);
            statusElement.textContent = 'Estado: ❌ Fallo en la conexión. Revisa la consola o intenta de nuevo.';
            alert('Fallo en la conexión. Asegúrate de que la dYdX Chain esté aprobada en Keplr.');
        }
    });
</script>

        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        header { background-color: #000000; color: #ffffff; padding: 40px 20px; text-align: center; }
        header h1 { font-size: 2.5em; margin-bottom: 10px; }
        .cta-button { background-color: #00bcd4; color: white; padding: 15px 30px; border: none; border-radius: 5px; font-size: 1.2em; cursor: pointer; transition: background-color 0.3s; }
        .cta-button:hover { background-color: #008fa3; }
        .steps, .details { padding: 40px 0; }
        .step-item { background-color: white; padding: 20px; border-radius: 8px; margin-bottom: 20px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        .step-item h3 { color: #00bcd4; }
    </style>
</head>
<body>

    <header>
        <h1>[Nombre del Proyecto]: Accede al Futuro Financiero Descentralizado</h1>
        <h2>Comercia, Gana e Interactúa con el Token $DYDX y la Red dYdX Chain.</h2>
        <p>$DYDX Valor actual: ≈ $0.25 USD</p>
        <button id="connectWalletBtn" class="cta-button">🚀 Empezar Ahora / Conectar Billetera</button>
    </header>

    <div class="container">

        <section class="steps">
            <h2>Guía Rápida: 3 Pasos para Empezar</h2>
            <div class="step-item">
                <h3>Paso 1: Conecta tu Billetera (Wallet)</h3>
                <p>Asegúrate de tener una billetera compatible. Haz clic en el botón de arriba y aprueba la conexión. **Es tu llave de acceso, segura y privada.**</p>
                <div id="walletStatus">Estado: Desconectado</div>
            </div>
            </section>

        </div>

</body>
</html>

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
